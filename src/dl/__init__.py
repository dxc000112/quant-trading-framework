"""
Deep Learning module for quantitative trading.

This package provides PyTorch-based models, datasets, and training utilities
as upgrades to the existing sklearn-based pipeline.

Modules:
    datasets    - Time-series DataLoader and data preprocessing
    models      - Neural network architectures (MLP, LSTM, CNN, Transformer)
    training    - Training loop, evaluation, and checkpointing
    utils       - Common utilities (device management, logging)
"""
